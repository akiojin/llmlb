# Comprehensive CI for Rust
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  # パス変更検出（C++ジョブの条件付き実行用）
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            rust:
              - 'llmlb/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '.github/workflows/ci.yml'

  rust:
    name: Rust (fmt/clippy/test) ${{ matrix.os }}
    needs: changes
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: No Rust-relevant changes
        if: needs.changes.outputs.rust != 'true'
        run: echo "No Rust-relevant changes; skipping Rust fmt/clippy/test."

      - uses: actions/checkout@v4
        if: needs.changes.outputs.rust == 'true'
        with:
          fetch-depth: 0
          submodules: recursive
      - name: Setup Rust
        if: needs.changes.outputs.rust == 'true'
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy
      - name: Bootstrap pest_generator
        if: needs.changes.outputs.rust == 'true'
        run: |
          if [ -f vendor/pest_generator/bootstrap/Cargo.toml ]; then
            cargo build --manifest-path vendor/pest_generator/bootstrap/Cargo.toml
          else
            echo "Skipping pest_generator bootstrap: vendor/pest_generator/bootstrap/Cargo.toml not found"
          fi
        shell: bash
      - name: Cargo fmt
        if: needs.changes.outputs.rust == 'true'
        run: cargo fmt --all -- --check
        shell: bash
      - name: Cargo clippy
        if: needs.changes.outputs.rust == 'true'
        run: cargo clippy --all-targets --all-features -- -D warnings
        shell: bash
      - name: Cargo test
        if: needs.changes.outputs.rust == 'true'
        run: cargo test --workspace --all-features -- --test-threads=1
        shell: bash

  markdownlint:
    name: Markdown Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: pnpm/action-setup@v4
        with:
          version: 10.20.0
      - name: Run markdownlint
        run: pnpm dlx markdownlint-cli2 "**/*.md" "!node_modules" "!.git" "!.github" "!.worktrees" "!vendor"

  coverage-rust:
    name: Coverage (Rust)
    needs: changes
    runs-on: ubuntu-latest
    steps:
      - name: No Rust-relevant changes
        if: needs.changes.outputs.rust != 'true'
        run: echo "No Rust-relevant changes; skipping coverage generation."

      - uses: actions/checkout@v4
        if: needs.changes.outputs.rust == 'true'
        with:
          fetch-depth: 0
          submodules: recursive
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        if: needs.changes.outputs.rust == 'true'
        with:
          toolchain: stable
      - name: Bootstrap pest_generator
        if: needs.changes.outputs.rust == 'true'
        run: |
          if [ -f vendor/pest_generator/bootstrap/Cargo.toml ]; then
            cargo build --manifest-path vendor/pest_generator/bootstrap/Cargo.toml
          else
            echo "Skipping pest_generator bootstrap: vendor/pest_generator/bootstrap/Cargo.toml not found"
          fi
        shell: bash
      - name: Install cargo-llvm-cov
        if: needs.changes.outputs.rust == 'true'
        run: |
          rustup component add llvm-tools-preview
          cargo install cargo-llvm-cov --locked
      - name: Run coverage
        if: needs.changes.outputs.rust == 'true'
        run: |
          mkdir -p coverage-rust
          cargo llvm-cov --all-features --workspace --lcov --output-path coverage-rust/lcov.info
      - name: Upload coverage artifact
        if: needs.changes.outputs.rust == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: rust-lcov
          path: coverage-rust/lcov.info
